# Sirini Jewellery — Daily Audit Report
**2026-07-20**

Repo location note: `D:\Owner\Desktop\Claude\Sirini_Website` does not exist yet — the repo is still at `D:\Owner\Desktop\Sirini_Website`, so this audit (and report) ran there.

## Summary

| Section | Findings | Auto-fixed & deployed | Needs manual action |
|---|---|---|---|
| SEO / AEO / GEO | 3 | 2 | 1 |
| Code Bugs | 3 | 0 | 3 (none urgent) |

**Headline:** this codebase already carries the scars of several prior hardening passes (checkout money-path fixes, timing-safe auth/webhook comparisons, JSON-LD determinism, rate limiting on every public POST route). `npx tsc --noEmit`, `npx eslint`, and a full production build (`DOTENV_CONFIG_PATH=.env.local npm run build`) all passed clean before and after the fix below. One real SEO bug was found, fixed, and **deployed to production with verified evidence**. No critical or high-severity code bugs were found.

---

## SEO / AEO / GEO

| # | File | Issue | Severity | Status |
|---|---|---|---|---|
| 1 | [app/shop/page.tsx](app/shop/page.tsx) | `generateMetadata()`'s facet-detection (`primaryFacets`) only recognized `category`/`occasion`/`style`/`collection`. The three newer taxonomy filters — `?look=`, `?stone=`, `?colour=` — already render unique breadcrumb nav, unique `ItemList` JSON-LD, and a unique on-page facet heading, but every one of those pages was silently emitted with `robots: noindex` and a generic duplicate title/description ("Shop Handcrafted Indian Jewellery…"). That's real, unique, crawlable catalog content (e.g. "Western Jewellery", "Kemp Stone Jewellery") that Google/AI engines were being told to ignore. | 🔴 **High** | 🟢 **Fixed & deployed** |
| 2 | [app/sitemap.ts](app/sitemap.ts) | Sitemap included facet URLs for `category`/`occasion`/`style`/`collection` but not `look`/`stone`/`colour`, even though the shop page fully supports those filters (per `getProducts()`). Once finding #1 made those pages indexable, they also needed sitemap entries so crawlers discover them without relying on internal links alone. | 🟡 Medium | 🟢 **Fixed & deployed** (bundled with #1) |
| 3 | [prisma/schema.prisma:261](prisma/schema.prisma) / [components/HeroCarousel.tsx:76-79](components/HeroCarousel.tsx) | `HeroSlide` has no admin-editable alt-text/caption field, so every hero slide's `alt` attribute is hardcoded to `"Sirini Jewellery — handcrafted necklace set"` regardless of what's actually pictured (owner could feature earrings, bangles, etc. and the alt text would be wrong). Fixing this properly needs a Prisma migration + admin UI field, which is a schema/design change, not a safe drop-in fix. | 🟡 Medium | ⚪ **Needs manual action** (schema change — flag for a future session if the owner wants it) |

**Verified live after deploy** (`curl https://sirinijewellery.com/shop?look=western`):
- `<title>Western Jewellery — Shop the Look | Sirini Jewellery</title>`
- `<meta name="robots" content="index, follow"/>`
- `<link rel="canonical" href="https://sirinijewellery.com/shop?look=western"/>`
- `sitemap.xml` now lists all `look=`/`stone=`/`colour=` term URLs.

**Everything else audited and found already solid (no changes needed):**
- `robots.ts` — correctly disallows `/admin/`, `/api/`, `/cart`, `/checkout`, `/account`, `/wishlist`, `/order-confirmation`, `/login`, `/register`; references both `sitemap.xml` and `image-sitemap.xml`.
- `llms.txt` (`app/llms.txt/route.ts`) — present, well-formed, category/style slugs match the live taxonomy constants.
- `image-sitemap.xml` — correctly wraps every product/blog image through `botImageUrl()` (avoids raw-original bandwidth burn, per the project's own Cloudinary gotcha).
- `components/ProductJsonLd.tsx` / `OrganizationJsonLd.tsx` / `FAQJsonLd.tsx` / `BreadcrumbJsonLd.tsx` — all deterministic (no `Date.now()`/`Math.random()` in render output — `priceValidUntil` is anchored to `${year+1}-12-31}`, review dates come from DB `createdAt`), and all escape `<` in their `dangerouslySetInnerHTML` output to prevent stored-XSS via admin/user content breaking out of the `<script>` tag.
- Product page (`app/shop/[slug]/page.tsx` + `ProductDetailClient.tsx`) — single `<h1>` with product name, per-product unique meta description (leads with name+category+price so it can never collide across products), owner-overridable `metaTitle`/`metaDescription`, full alt text on gallery images (`"{name} — image N of M"`, `"...— back view"`), canonical URL, breadcrumb JSON-LD.
- FAQ page — on-page accordion and `FAQPage` JSON-LD both read the same `getFaq()` source, so they can't drift apart (real AEO win — clean Q&A pairs Google/AI engines can lift directly).
- Homepage, shop listing, about/contact/blog — one `<h1>` each, confirmed via static scan of all top-level `page.tsx` files.
- `/cart` — correctly `noindex`ed via `app/cart/layout.tsx` (a `"use client"` page can't export `metadata` itself, but the co-located layout does it correctly — false alarm on first pass).
- Canonical/duplicate-content handling on `/shop`: multi-filter combos and `page>1` already correctly get `noindex,follow` to avoid thin-content indexing.

---

## Code Bugs

| # | File | Issue | Severity | Status |
|---|---|---|---|---|
| 1 | `npm audit` | 5 moderate advisories, all in dev/build tooling only (`@prisma/dev`'s bundled `@hono/node-server`, `postcss` via `next`'s vendored copy). No high/critical. The only `fixAvailable` path is a **semver-major downgrade** of `prisma`/`next` — applying it would be a regression, not a fix, and none of the flagged code paths run in the production request path (dev-server-only / build-time CSS stringify). | 🟡 Medium (informational) | ⚪ **Needs manual action** — monitor for a proper patch release; do not downgrade |
| 2 | `app/api/admin/**` (20 route files, 27 HTTP method handlers checked) | Verified **every** admin API route — `products`, `orders/[id]/status`, `products/upload`, `taxonomy/*`, `coupons`, `hero`, `blog`, `categories`, `admins`, `settings`, `account`, `messages` — has exactly one `session?.user?.isAdmin` check per exported method, returning `403` on failure. No gaps found. | — | 🟢 **OK, verified** |
| 3 | `lib/auth.ts`, `app/api/webhooks/razorpay/route.ts`, `app/api/cron/daily-digest/route.ts`, `lib/apiKeyAuth.ts` | Spot-checked the highest-value attack surfaces: login uses a dummy-hash bcrypt compare to prevent timing-based user enumeration; Razorpay webhook verifies HMAC-SHA256 signature with `crypto.timingSafeEqual`; cron digest fails closed (401) in production if `CRON_SECRET` is unset; machine-to-machine routes (`marketing-metrics`, `leads/coupon`) use a shared-secret header checked with `timingSafeEqual` and fail closed if the env var is missing. All correct. | — | 🟢 **OK, verified** |

**Also checked, no issues found:**
- `npx tsc --noEmit` — clean, both before and after the fix.
- XSS: searched every `dangerouslySetInnerHTML` use (12 files) — all are JSON-LD `<script>` blocks with `<` escaping; blog article body content (`article.body[].paragraphs`) renders through plain JSX text interpolation (React auto-escapes), not raw HTML.
- File upload (`app/api/admin/products/upload/route.ts`) — admin-gated, MIME allowlist explicitly excludes `image/svg+xml` (SVG can embed scripts), 5MB size cap, uploads go through Cloudinary (no local filesystem write).
- Checkout money path (`app/api/checkout/create-order/route.ts`) — server re-fetches prices/stock from DB (never trusts client-submitted amounts), zod-validates the full body, coupon validated server-side (active/expiry/usage-cap/min-order), shared `computeTotals()` used for both client display and server charge so they can't diverge.
- Rate limiting — present on every public mutation route checked (`contact`, `newsletter`, `checkout/*`, `coupon/validate`, `leads*`, `orders/[id]/cancel`, `reviews`, `wishlist*`, `addresses*`, `pincode`, `products/[slug]`, `search`, `auth/register`) via `lib/rateLimit.ts` (in-memory fixed-window, scoped per endpoint + IP).
- Secret exposure — grepped for server-only env vars (`RAZORPAY_KEY_SECRET`, `RESEND_API_KEY`, `CRON_SECRET`, `DATABASE_URL`, `MARKETING_METRICS_API_KEY`, `RAZORPAY_WEBHOOK_SECRET`) inside `.tsx` files — zero matches, nothing leaking into the client bundle.
- CSRF — NextAuth v5 JWT session cookies default to `SameSite=Lax`, which blocks the cookie on cross-site state-changing requests; no gap found requiring a separate token scheme.

---

## Deploy record

- Commit: `90ca84c` — *"fix: index look/stone/colour shop facet pages, add to sitemap"*
- `npx tsc --noEmit` ✅ clean · `npx eslint app/shop/page.tsx app/sitemap.ts` ✅ clean · `DOTENV_CONFIG_PATH=.env.local npm run build` ✅ succeeded (452 routes, no errors)
- Pushed to `main`, Vercel auto-deploy confirmed live (~3.5 min), verified via `curl` against production as shown above.
- No other files were touched or committed. (Pre-existing unrelated uncommitted changes in the working tree — `.claude/launch.json`, `.claude/skills/deploy-and-verify/SKILL.md`, various untracked scratch folders — were left untouched, as instructed.)
