# Sirini Jewellery — Daily Audit Report
**2026-07-22**

Repo location note: `D:\Owner\Desktop\Claude\Sirini_Website` does not exist yet — the repo is still at `D:\Owner\Desktop\Sirini_Website`, so this audit (and report) ran there.

## Summary

| Section | Findings | Auto-fixed & deployed | Needs manual action |
|---|---|---|---|
| SEO / AEO / GEO | 0 new | — | 0 new (3 recurring, unchanged — see prior reports) |
| Code Bugs | 1 new (informational) | 0 | 1 new (low, optional) + 1 recurring (`postcss`/`next` advisory, unchanged) |

**Headline:** No code changes were needed this run. Since the last audit ([report-2026-07-21.md](report-2026-07-21.md)), four commits landed (`93da77c`…`57dbc3d` range: `14a05f1` Speed Insights, `8d99008` real welcome-coupon-in-popup, `8b5ddbb` docs, `57dbc3d` workspace-artifacts commit). All four were read in full and are clean: the new lead-coupon mint path (`lib/mintLeadCoupon.ts`, `app/api/leads/route.ts`, `app/api/leads/coupon/route.ts`) is rate-limited, zod-validated, idempotent per email (handles the concurrent-mint race via a `P2002` re-read rather than trusting error-shape), and renders the coupon code as plain React text (no injection risk); `app/layout.tsx`'s new `<SpeedInsights />` mount is correctly placed and gated by nothing sensitive. The 14 new `Website documentation/*.md` files were grep-checked for accidentally-copied real secrets (API keys, tokens, private keys) — none found. `npx tsc --noEmit`, `npm audit`, and `npx eslint .` were all re-run fresh; the only new signal is 13 pre-existing ESLint *warnings* (0 errors) in files untouched by recent commits, logged below as a low-severity, non-blocking cleanup item.

---

## SEO, AEO & GEO

No new findings this run. Re-verified the following, all still correct (no drift since 2026-07-21):

| File / Area | Check | Result |
|---|---|---|
| `app/sitemap.ts` | Deterministic `lastModified` (newest product/article, never `Date.now()`); category/occasion/style/collection/look/stone/colour facet routes present, deduped by URL | 🟢 OK |
| `components/ProductJsonLd.tsx` | `priceValidUntil` still anchored to `${year+1}-12-31` (deterministic, ISR-safe, re-read this run line-by-line); `<` escaped in `dangerouslySetInnerHTML` (stored-XSS guard); return-window / shipping facts match FAQ | 🟢 OK |
| `app/layout.tsx` | New `<SpeedInsights />` mount alongside existing `<Analytics />`; JSON-LD (`WebSiteJsonLd`, `LocalBusinessJsonLd`), verification meta, viewport theme-color all unaffected by the change | 🟢 OK |
| `Website documentation/*.md` (new, 14 files) | Not served publicly (repo root, outside `app/`/`public/`) — no SEO surface impact either way; grepped for leaked secrets, none found | 🟢 OK |
| `app/robots.ts`, `app/llms.txt/route.ts` | Spot-checked, unchanged since last audit, still correct | 🟢 OK |

**Recurring, unchanged from prior runs (owner decision needed, not a drop-in fix):**
- FAQ frames the return window as a defects/wrong-item "exchange"; Shipping frames the same window as a general "return" — day-counts match, semantic framing still differs.
- A developer-credit "Who made this website?" Q&A still sits inside the customer-facing `FAQPage` schema.
- `HeroSlide` has no per-slide alt-text/caption field in the schema — hero `alt` text is hardcoded regardless of what's pictured. Needs a Prisma migration + admin UI field.

---

## Code Bugs

| # | File | Issue | Severity | Status |
|---|---|---|---|---|
| 1 | `components/CartDrawer.tsx:288`, `components/Navbar.tsx:379`, `components/admin/TaxonomyCategoriesClient.tsx:196` | Plain `<img>` tags instead of `next/image`/the Cloudinary loader — flagged by `@next/next/no-img-element` (slower LCP, no automatic optimization/bandwidth savings on these specific images). Pre-existing, not introduced by any recent commit. | 🟢 Low (perf hint, no correctness/security impact) | Needs manual action — **not fixed**: swapping to `next/image` in `Navbar.tsx`/`CartDrawer.tsx` risks visual/layout changes (sizing, `object-fit`, cropping behavior), which this audit's scope explicitly excludes without owner sign-off. |
| 2 | `components/Navbar.tsx:122`, `components/WishlistButton.tsx:25` | `react-hooks/exhaustive-deps` — two `useEffect`s with intentionally-omitted dependencies (`ANNOUNCEMENTS.length`, `session?.user`). Likely deliberate (both look like "run once, re-check on a specific trigger" patterns), but unverified without deeper trace. | 🟢 Low (lint warning, no observed runtime bug) | Needs manual action — flagged for review, not changed (would require confirming intent before editing effect deps). |
| — | `npm audit` | `next`'s vendored `postcss` (`<8.5.10`, CSS-stringify XSS, CWE-79), moderate. Unchanged from every prior run. Only `fixAvailable` path is `npm audit fix --force`, which downgrades `next` to a `9.x` canary — a severe regression on this Next 16 codebase. | 🟡 Medium (no safe fix available) | Recurring — correctly left alone, needs a proper Next.js patch release upstream. |

**Also re-verified this run, no issues found:**
- `npx tsc --noEmit` — clean.
- `npx eslint .` — **0 errors**, 13 warnings (all pre-existing, in files untouched by the last 4 commits — see table above; first time this audit series has explicitly enumerated warnings rather than just checking the error count).
- `lib/mintLeadCoupon.ts` / `app/api/leads/route.ts` / `app/api/leads/coupon/route.ts` (new this cycle): rate-limited (`enforceRateLimit`, 30/10min public, 10/10min key-authed), zod-validated email/discount/expiry, idempotent per email (checked-then-create, with a `P2002`-triggered re-read to resolve the concurrent-mint race rather than string-matching Prisma's error shape), coupon code generated from `crypto.randomBytes` over a 32-char unambiguous alphabet (no modulo bias), minting failure on the public path fails open to `{ok:true}` without leaking internals, key-authed route unchanged in its external contract. No injection, no auth gap, no idempotency hole found.
- `components/LeadCapturePopup.tsx`: coupon code rendered as plain JSX text (React-escaped, no `dangerouslySetInnerHTML` involved), clipboard API wrapped in try/catch, storage access guarded, dialog-close-on-navigation still uses the render-time (not effect-time) state adjustment fixed on 2026-07-21 — confirmed not regressed by this session's coupon-UI changes.
- Admin API auth coverage: no new `app/api/admin/**` routes since 2026-07-21; existing coverage unchanged.
- Full picture cross-checked against `Website documentation/09-api-reference.md` and `11-security-and-performance.md` (the new docs) — both accurately describe the current auth/rate-limit posture with no factual drift from the actual code.

---

## Deploy record

**No deploy this run.** No code changes were made — every finding was either already-fixed (re-verified only) or a low-severity item explicitly left for manual review per the reasoning above. Working tree was clean at both the start and end of this run (`git status` — nothing to commit).

**Recurring open items (unchanged, not re-litigated):** FAQ "exchange" vs Shipping "return" semantic framing; developer-credit Q&A inside customer `FAQPage`; `HeroSlide` per-slide alt-text schema gap; `postcss`/`next` moderate advisory (needs a proper Next patch release, not a downgrade); login brute-force throttle (proposed and declined by the owner on 2026-07-02); 3 `<img>`-element lint warnings and 2 `exhaustive-deps` warnings noted above (new to this report, none blocking, none touched).
